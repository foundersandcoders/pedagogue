{
  "name": "rhea",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "generate:palettes": "node scripts/generatePaletteCss.js",
    "prebuild": "npm run generate:palettes",
    "dev": "npm run prebuild && vite dev --port 5173",
    "check-build": "npm run prebuild && npm run build && npm run preview",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@sveltejs/adapter-node": "^5.0.0",
    "@sveltejs/kit": "^2.47.3",
    "eslint": "^9.38.0",
    "svelte": "^5.41.0",
    "svelte-preprocess": "^6.0.3",
    "vite": "^6.4.1"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.20.0",
    "@langchain/anthropic": "^1.0.0",
    "@xmldom/xmldom": "^0.8.11",
    "langchain": "^1.0.1",
    "svelte-kit": "^1.2.0",
    "zod": "^3.25.76"
  },
  "type": "module"
}
